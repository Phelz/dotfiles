cd
lt
lt -h
ltree
source .zshrc
cd setup_machine
gic -m 'init'
gc -m 'init'
git cm 
git cm 'init'
gps origin main
git ps origin main
ga
gcm 'added readme'
git cm 'added readme'
g ps origin main
git ps origin main
cd dotfiles
..
zsh <(curl -s https://raw.githubusercontent.com/zap-zsh/zap/master/install.zsh) --branch release-v1
source
